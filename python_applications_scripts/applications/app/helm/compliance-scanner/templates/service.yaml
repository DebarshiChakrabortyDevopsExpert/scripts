apiVersion: v1
kind: Service
metadata:
  name: {{ include "compliance_scanner.fullname" . }}
  labels:
    {{- include "compliance_scanner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "compliance_scanner.selectorLabels" . | nindent 4 }}
