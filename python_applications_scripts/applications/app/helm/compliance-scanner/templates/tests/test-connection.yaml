apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "compliance_scanner.fullname" . }}-test-connection"
  labels:
    {{- include "compliance_scanner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "compliance_scanner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
